namespace Mapgenix.GSuite.Android
{
    /// <summary>
    /// Enumeration representing the lock mode while drawing layers.
    /// </summary>
    public enum LockLayerMode
    {
        Default = 0,
        Lock = 1,
        DoNotLock = 2
    }
}
