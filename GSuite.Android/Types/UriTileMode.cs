namespace Mapgenix.GSuite.Android
{
    /// <summary>
    /// Enumeration for the tile type in the tile overlay.
    /// </summary>
    public enum UriTileMode
    {
        Default = 0,
        Custom = 1
    }
}
