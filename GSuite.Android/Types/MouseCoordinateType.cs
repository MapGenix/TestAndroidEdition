namespace Mapgenix.GSuite.Android
{
    /// <summary>
    /// Enumeration for the MousePostion Type that appears when the pointer is moving on the map.
    /// </summary>
    public enum MouseCoordinateType
    {
        Default = 0,
        LongitudeLatitude = 1,
        LatitudeLongitude = 2,
        DegreesMinutesSeconds = 3,
        Custom = 4
    }
}