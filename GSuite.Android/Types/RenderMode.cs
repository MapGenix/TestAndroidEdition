namespace Mapgenix.GSuite.Android
{
    /// <summary>Enumeration for the drawing mode when rendering map.</summary>
    public enum RenderMode
    {
        Default = 0,
        GdiPlus = 1,
        DrawingVisual = 2
    }
}
