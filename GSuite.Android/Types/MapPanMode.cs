namespace Mapgenix.GSuite.Android
{
    /// <summary>
    /// Enumeration for the behavior type for panning.
    /// The default value is StandardPanning.
    /// </summary>
    public enum MapPanMode
    {
        Default = 0,
        StandardPanning = 1,
        Disabled = 2
    }
}